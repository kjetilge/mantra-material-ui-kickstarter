const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../user_controls';

describe('app.actions.user_controls', () => {
  it('should do something');
});
